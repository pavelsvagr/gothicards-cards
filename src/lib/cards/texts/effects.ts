import { Quote } from '../../formatting'

const descriptions: { [slug: string]: Quote | string } = {
  // C
  courtship: 'There are several different rituals scavengers use for finding a mate including singing, dancing, feeding and preening.',
  // D
  diggersMeat: 'There are many bad smelling foods out there, though the onese that also taste great are a few. This mushroom is the case.',
  // H
  healingRoot: 'There is no healing elixir without a healing root.',
}

export default descriptions
