import { Quote } from '../../formatting'

const descriptions: { [slug: string]: Quote | string } = {
  // A
  abandonedMine: 'Once upon a time, there was a life. Now the only thing you can find here is death and some not mined ore.',
  // O
  oldCave: 'Place that only fool can visit alone.',
}

export default descriptions
